<div
  class="bg-gradient-to-tr from-black via-black to-[#1c6704] flex flex-col items-center justify-center text-center gap-1 border-transparent relative overflow-hidden"
>
  <!-- <div class="bg-red-600 text-white font-extrabold py-4 w-full text-center">
    <div class="max-w-6xl mx-auto px-4">
      <p class="mb-2">🔥😱 SÓ HOJE - ATÉ 60% DE DESCONTO</p>
      <p class="text-md bg-white text-red-600 rounded-lg p-1">
        ⏰ Termina em: <span id="countdown" class="">--:--:--</span>
      </p>
    </div>
  </div> -->

  <img src="/LogoiGenda.png" alt="Logo iGenda" class="w-20 mb-4 mt-4" />

  <h1
    class="text-4xl sm:text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight mb-2 text-white"
  >
    Torne-se um profissional organizado e lucrativo
  </h1>

  <p class="text-xl sm:text-xl max-w-2xl sm:max-w-3xl text-white px-4 mb-4">
    Facilite agendamentos, reduza ausências, envie lembretes automáticos,
    recupere seus cliente e mantenha sua agenda sempre cheia.
  </p>

  <div
    class="w-full sm:w-80 md:w-[500px] lg:w-[600px] xl:w-[1000px] px-4 rounded-xl sm:mb-8 shadow-lg overflow-hidden"
  >
    <video
      src="/DemoiGenda.mp4"
      loop
      playsinline
      controls
      class="w-full h-auto rounded-xl"
      poster="/MockupiGenda.webp"
    >
    </video>
  </div>

  <div class="flex flex-col gap-1 items-center mb-4">
    <h2 class="text-lg text-white mb-2">
      Por apenas: <br />
      <span class="text-green-500 text-7xl font-extrabold mt-2">R$ 27,99</span>
      <span class="text-xs text-gray-300">/mês</span>
    </h2>
  </div>

  <a href="https://pay.cakto.com.br/ht897wv_511991" class="scroll-link">
    <button
      data-fb-event="PlanoMensalClicado"
      class="zoom-in-out mb-4 bg-green-500 text-black p-4 rounded-md shadow transition text-xl sm:text-base font-semibold flex items-center gap-1"
    >
      <img src="/IconButton.png" alt="Seta para a direita" class="w-8 h-8" />
      Quero organizar minha agenda!
    </button>
  </a>

  <svg
    xmlns="http://www.w3.org/2000/svg"
    width="54"
    height="54"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    class="lucide lucide-arrow-down-icon lucide-arrow-down mb-10 text-white font-extrabold"
    ><path d="M12 5v14"></path><path d="m19 12-7 7-7-7"></path></svg
  >

  <script>
    function updateCountdown() {
      const now = new Date();
      const endOfDay = new Date();
      endOfDay.setHours(23, 59, 59, 999);

      const timeLeft = endOfDay.getTime() - now.getTime();

      if (timeLeft > 0) {
        const hours = Math.floor(timeLeft / (1000 * 60 * 60));
        const minutes = Math.floor((timeLeft % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((timeLeft % (1000 * 60)) / 1000);

        const countdownElement = document.getElementById("countdown");
        if (countdownElement) {
          countdownElement.textContent = `${hours.toString().padStart(2, "0")}:${minutes.toString().padStart(2, "0")}:${seconds.toString().padStart(2, "0")}`;
        }
      } else {
        // Se já passou do fim do dia, mostra 00:00:00
        const countdownElement = document.getElementById("countdown");
        if (countdownElement) {
          countdownElement.textContent = "00:00:00";
        }
      }
    }

    // Atualiza o contador a cada segundo
    updateCountdown();
    setInterval(updateCountdown, 1000);
  </script>
</div>
